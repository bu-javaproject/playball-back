spring:
  datasource:
    url: jdbc:mysql://localhost:3306/playball?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: jshz20221127@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

jwt:
  secret: playball-secret-key-must-be-at-least-64-characters-long-for-security
  access-expiration: 1800000
  refresh-expiration: 604800000

server:
  port: 8080